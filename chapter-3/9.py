# Show d(n) is O(f(n)), then ad(n) is O(f(n)), for any constant a > 0:
# 
# d(n) <= f(n)
# ad(n) <= af(n)
#
# by definition:
#   g(n) is O(f(n)) if g(n) <= cf(n) while c > 0
#
# thus:
# ad(n) is O(f(n)))