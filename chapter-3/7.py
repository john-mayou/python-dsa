# (a): says that for all inputs, the runtime is bounded by O(f(n))
# (b): says that for the worse case, the runtime is bounded by O(f(n))
# Both statements are the same, although Big O usually uses the first to describe its performance.